import { CieloEmojiPipe } from './cielo-emoji.pipe';

describe('CieloEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new CieloEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
